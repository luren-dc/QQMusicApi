[tool.poetry]
name = "qqmusicapi"
version = "0.1.0"
description = ""
authors = ["luren <3308862290@qq.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8.1"
requests = "^2.31.0"
click = "^8.1.3"
flask = "^2.3.2"
watchdog = "^3.0.0"
flask-caching = "^2.0.2"

[tool.poetry.scripts]
app = 'qqmusicapi.app:main'

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.isort]
profile = "black"
